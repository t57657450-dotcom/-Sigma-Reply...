const express = require("express");
const OpenAI = require("openai");
const path = require("path");
require("dotenv").config();

const app = express();

const openai = new OpenAI({
    apiKey: process.env.OPENAI_API_KEY
});

app.use(express.json({ limit: "20kb" }));

app.use(express.static(path.join(__dirname, "public")));

app.post("/api/reply", async (req, res) => {
    try {
        const message =
            typeof req.body.message === "string"
                ? req.body.message.trim()
                : "";

        const style =
            typeof req.body.style === "string"
                ? req.body.style
                : "sigma";

        if (!message) {
            return res.status(400).json({
                error: "پیام خالی است."
            });
        }

        const styles = {
            sigma: `
لحن سیگما، خونسرد، بااعتمادبه‌نفس و کوتاه.
بدون فحش سنگین و بدون تهدید.
`,

            kind: `
لحن مهربان، دوستانه و حاضرجواب.
`,

            funny: `
لحن خنده‌دار، بامزه و حاضرجواب.
`,

            cool: `
لحن خفن، محکم، بااعتمادبه‌نفس و جذاب.
`,

            polite: `
لحن کاملاً محترمانه ولی حاضرجواب.
`,

            short: `
جواب خیلی کوتاه و مستقیم، ترجیحاً یک جمله.
`
        };

        const selectedStyle =
            styles[style] || styles.sigma;

        const instructions = `
تو موتور هوش مصنوعی سایت «🗿 Sigma Reply» هستی.

وظیفه تو پاسخ دادن به سؤال نیست.

وظیفه تو ساختن «حاضرجوابی» است.

کاربر حرف شخص دیگری را به تو می‌دهد.
تو باید معنی، منظور و لحن آن پیام را بفهمی
و یک جواب حاضرجواب و مرتبط با همان حرف بسازی.

اگر طرف تعریف کرد:
به همان تعریف یک جواب حاضرجواب بده.

اگر طرف مسخره کرد:
یک جواب حاضرجواب و قاطع به همان حرف بده.

اگر طرف طعنه زد:
یک جواب متناسب با همان طعنه بده.

اگر طرف چالش کرد:
یک جواب خونسرد و بااعتمادبه‌نفس بده.

اگر طرف سؤال پرسید:
سؤال را علمی یا آموزشی جواب نده.
به خود سؤال یک جواب حاضرجواب بده.

مثال:

پیام:
«تو چقدر مهربونی»

جواب:
«بالاخره یکی فهمید 😎»

پیام:
«خیلی ادعایی»

جواب:
«ادعا نیست، فقط لازم نیست همه‌چیو ثابت کنم 🗿»

پیام:
«فکر کردی خیلی خفنی؟»

جواب:
«نه، فقط برای خفن بودن اجازه نمی‌گیرم 😎»

پیام:
«تو هیچی نیستی»

جواب:
«پس چرا هنوز داری درباره من حرف می‌زنی؟ 🗿»

قوانین:

- جواب باید دقیقاً به همان پیام مرتبط باشد.
- جواب بی‌ربط و کلیشه‌ای نساز.
- اگر پیام مبهم بود، باز هم یک جواب بساز.
- هیچ‌وقت نگو «نمی‌دانم».
- تحلیل یا توضیح اضافه نده.
- فقط خود جواب حاضرجواب را برگردان.
- معمولاً یک جمله کافی است.
- فحش سنگین، تهدید یا تشویق به آسیب ممنوع
